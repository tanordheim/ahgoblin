<% if item_component.errors.any? -%>
  $.setModalBody("<%= escape_javascript(render('form')) %>");
  $.setItemReagentPriceFieldStatus();
<% else -%>
  $('#components-container').append("<%= escape_javascript(render(item_component)) %>");
  $('#components-container tr:last').highlight();
  $.closeModal();
<% end -%>
