= simple_form_for transformation_reagent, :remote => true do |f|

  = render_form_errors f

  = field_set_tag do
    = f.association :reagent, :collection => reagents, :include_blank => false, :wrapper => :inline
    = f.input :quantity, :wrapper => :inline
