%tr
  %td.name= link_to transformation.name, edit_transformation_path(transformation)
  %td.delete
    = link_to '&times;'.html_safe, transformation_path(transformation), :method => :delete, :confirm => 'Are you sure you want to remove this transformation?'
